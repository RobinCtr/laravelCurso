<section> 
     <h2 class="text-center">{{ $title }}</h2>
        <div class="alert alert-{{ $type }} text-center" role="alert">
            <strong>{{ $slot }}</strong>
        </div>
        <h1> Hola {{ $name}} </h1>
</section>

